# GENERATED BY CHATGPT
name: Delete Staging Branch

on:
  workflow_dispatch: # for manual triggering
    inputs:
      branchName:
        description: 'Input your branch you want to delete'
        required: true
        type: string
  # schedule:
  #   - cron: '5 * * * *'
  # disabled schedule for healtier

jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout the Action
        uses: actions/checkout@v2
      - name: 2. Set Specific Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16.x'
      - name: 3. Deleting Branch
        uses: dawidd6/action-delete-branch@v3
        with:
          github_token: ${{ secrets.ACCESS_TOKEN }}
          branches: ${{ inputs.branchName }}
