# GENERATED BY CHATGPT
name: Delete Staging Branch

on:
  workflow_dispatch: # for manual triggering
    inputs:
      branchName:
        description: 'Input your branch you want to delete'
        required: true
        type: string
  # schedule:
  #   - cron: '5 * * * *'
  # disabled schedule for healtier

jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Delete Branch
        run: echo "your input is ${{ inputs.branchName }}"
