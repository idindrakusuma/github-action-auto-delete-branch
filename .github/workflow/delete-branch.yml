# this code generated by chatGPT
name: Run on Specific Date

on:
  workflow_dispatch:
  schedule:
    - cron: "0 19 * * * 2023"

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run command
        run: echo "Hello, world!"